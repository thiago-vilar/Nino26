"""Runnable project entry points.

Keeping ``scripts`` as an explicit package makes both supported invocation
styles deterministic: ``python scripts/<runner>.py`` and
``python -m scripts.<runner>``.
"""
